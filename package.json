{
  "name": "template-tools",
  "version": "1.0.0",
  "description": "A command tool.To generate some template,autodownload some dependencies.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "quick-template-tool": "main.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "commander": "^11.1.0"
  }
}
